package game.entities;

public abstract class EnemyB extends Enemy{
    //Atributos

    //Constructor
    public EnemyB(double x, int speed, int maxHealth, int health, int damage, double heigth, double width, double y) {
        super(x, speed, maxHealth, health, damage, heigth, width, y);
    }
}
